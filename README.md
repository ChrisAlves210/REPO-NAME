# REPO-NAME

## Associate a remote and push

1. Add your target repository as a remote:
   ```bash
   git remote add assignment <your-repository-url>
   ```
2. Push your current branch to that remote:
   ```bash
   git push -u assignment HEAD
   ```